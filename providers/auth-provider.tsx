"use client";

import { AuthProvider } from '@/contexts/auth-context';

/**
 * Re-export AuthProvider for convenience
 * This allows importing from providers/auth-provider instead of contexts/auth-context
 */
export { AuthProvider };

